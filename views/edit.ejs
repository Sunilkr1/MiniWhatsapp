<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit your chat</title>
</head>
<body>
    <h3>Edit your chat: <%= chat._id %></h3>
    <p>This chat was sent from <%= chat.from %> to <%= chat.to %></p>
    <form action="/chats/<%= chat._id %>?_method=PUT" method="POST">
        <textarea name="msg" rows="5" cols="30"><%= chat.msg %></textarea>
        <br>
        <button type="submit">Edit</button>
        <br>
    </form>
    <form action="/chats/<%= chat._id %>?_method=DELETE" method="POST" style="margin-top: 20px;">
        <button type="submit" style="background-color: red; color: white;">Delete</button>
    </form>
</body>
</html>
